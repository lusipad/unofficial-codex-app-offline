# skill-installer 配置文件
#
# 用法：将本文件复制一份，去掉 .example 后缀，按需修改。
# 搜索顺序（先找到先生效）：
#   1. <安装目录>\skill-installer.env                 ← 推荐（与 .cmd 文件并排）
#   2. %USERPROFILE%\.codex\skill-installer.env       （用户级配置）
#
# 规则：
#   - 以 # 开头的行为注释，忽略空行
#   - 系统环境变量优先级更高，不会被此文件覆盖
#   - 路径中无需转义反斜杠（直接写 D:\path\to\dir 即可）


# ── GitHub 镜像 / 内网配置 ──────────────────────────────────────────────────

# GitHub 网页及 git clone 的根地址（默认：https://github.com）
# 同时影响 --url 参数的 URL 校验
#CODEX_GITHUB_BASE=https://github.company.local

# GitHub REST API 根地址（默认：https://api.github.com）
# GitHub Enterprise 通常为 https://<host>/api/v3
#CODEX_GITHUB_API_BASE=https://api.github.company.local

# zip 下载根地址（默认：自动从 CODEX_GITHUB_BASE 推导）
# 若镜像的 codeload 地址与 GitHub 子域名规律不同，才需要单独设置
#CODEX_CODELOAD_BASE=https://codeload.company.local


# ── 认证 ────────────────────────────────────────────────────────────────────

# GitHub / 镜像站的访问 Token（可选，用于私有仓库或解除 API 速率限制）
#GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx


# ── 本地 skills 来源 ─────────────────────────────────────────────────────────

# skill 安装/列表时优先使用的本地目录（默认：离线包自动设置为内置 seed 目录）
# 当使用离线包启动时，bootstrap 会自动将此项设置为 _internal\seed\codex-home\skills，
# 无需手动配置；仅在需要指向其他目录时才取消注释并修改。
# 设置后，skill 安装器将跳过 GitHub，直接从本目录加载 skills。
# 如需强制走 GitHub，保持此项注释即可（或在命令行显式指定 --url/--repo）。
#CODEX_SKILL_SOURCE_DIR=D:\my-skills


# ── 路径 ────────────────────────────────────────────────────────────────────

# Codex 主目录（默认：%USERPROFILE%\.codex）
# 修改此项会同时改变 skills 的安装位置
#CODEX_HOME=D:\.codex
